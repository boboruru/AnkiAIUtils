# 交接檔：Anki 複習小幫手 GUI 開發進度

> 最後更新 2026-06-08，給接手的新 Claude 視窗閱讀

## 1. 現在的現況

### 已完成（本輪新增 / 修正）
- **`review_helper_gui.py` 已可正常運作並修掉數個永久性 bug**：
  1. **空白回應誤判完成**：LLM 偶爾回傳空白內容卻被標記成 ✅ 完成、欄位卻是空的。已在 `_worker_loop` 加上空字串檢查，回傳空白時直接拋例外、顯示 ❌ 錯誤，不寫入筆記
  2. **狀態列縮小視窗會消失**：原因是 Treeview 比狀態列先 `pack()`、搶走了縮放空間。已改成狀態列用 `side="bottom"` 優先 pack，中間表格縮放時才會被壓縮
  3. **Markdown 條列擠成一團**：`markdown.markdown()` 加上 `nl2br` extension，讓單一換行也轉成 `<br />`，避免瀏覽器把 `\n` 摺疊掉
  4. **固定標籤粗體不一致**：LLM 有時會自動把「意思：」「文法要點：」「多益考點：」等加粗、有時不會。新增 `LABEL_RE` + `bold_fixed_labels()`，在寫回前統一強制轉成 `**標籤**` 格式（注意：正則必須是冪等的，已驗證對已加粗的輸入跑兩次結果相同，不會產生 `****`）
- **新增「本機額度估算」功能**（`usage_count.json`，已加入 `.gitignore`）：
  - 每次成功呼叫 LLM 就計數 +1，跨日（依 UTC 重置時間）自動歸零
  - 顯示在底部狀態列：`📊 今日已用 N/500 次（本機估算）`
  - 純本機計算，不額外消耗任何 API/Token 額度
- **新增「視窗大小與位置記憶」功能**（`window_geometry.json`，已加入 `.gitignore`）：
  - 關閉視窗時記錄目前 geometry 字串，下次啟動自動還原
- **Anki 卡片樣式調整（已備份原始 CSS/模板於 `backups/`，已加入 `.gitignore`）**：
  - 把 AI 產生的解析內容（`{{中文}}`、`{{文法結構}}`、`{{重點單字}}`、`{{相似例句}}`）從置中改成靠左對齊（`text-align: left`），維持原本標題/徽章置中設計

### 尚未完成 / 進行中
- **使用者尚未完成完整的 GUI 端到端互動測試**（見下方測試清單）
- **「記錄檔」功能仍卡在使用者澄清**：要記錄「哪一天分析了哪些題目（含題組、題號）」，已查證卡片有「題目編號」欄位、音檔檔名格式為 `題組_TOEIC_Listening_題號.mp3`，但題組判斷方式、欄位設計、檔案格式（CSV / 純文字 / Markdown）三點仍待使用者回覆，**尚未開始實作**

### 重要設定值（寫死在 `review_helper_gui.py` 開頭）
```python
MODEL = "gemini/gemini-3.1-flash-lite"          # 每日額度 500，目前帳號可用模型中最高
DATASET_PATH = "examples/toeic_dataset.txt"     # 多益專屬範本
LISTENING_NOTE_TYPE = "Parker 聽力用(2026)"      # 筆記類型名稱（使用者自訂）
DAILY_LIMIT = 500                                # 對應 GEMINI_QUOTA.md 中 3.1 Flash Lite 的 RPD
```
寫回欄位規則（已內嵌在 `write_explanation_to_card()`）：
- 「中文」欄位是空的 → 寫入「中文」
- 「中文」欄位已有內容 → 改寫入「文法結構」
- 寫入後一律清空 `AnkiExplainer` 欄位

### 使用者偏好（務必注意）
- **對話請全程使用繁體中文**
- 啟動指令：
  ```powershell
  cd "<本機專案路徑>\AnkiAIUtils"
  $env:PYTHONUTF8="1"
  .\env\Scripts\python.exe review_helper_gui.py
  ```
  **必須**用 `.\env\Scripts\python.exe`（虛擬環境），否則會報 `ModuleNotFoundError: No module named 'py_ankiconnect'`

## 2. 接下來要做的測試

1. 開始複習「Parker 聽力用(2026)」筆記類型、且「中文」欄位是空的卡片，確認 GUI 上方預覽文字約 1 秒內更新
2. 點擊「📥 加入分析佇列」，確認清單出現新項目（⏳ 待處理 → ⚙️ 處理中），狀態列數字同步變化
3. 等待約 1～1.5 分鐘，確認狀態變成 ✅ 完成、耗時欄位顯示秒數，且「今日已用」計數 +1
4. 回到 Anki 重新整理該卡，確認「中文」欄位已寫入格式化內容（靠左對齊、標籤粗體、條列分行正確）、`AnkiExplainer` 欄位已清空
5. 換一張「中文」欄位已有內容的卡測試，確認改寫進「文法結構」欄位、「中文」維持不動
6. 重複點擊同一張卡（待處理/處理中），確認被擋下並顯示提示
7. 對已完成的卡再次點擊，確認跳出「要不要重新分析」確認對話框
8. 切到牌組列表（離開複習畫面），確認預覽顯示「（未在複習中）」
9. 縮小視窗高度，確認狀態列維持在底部不消失，中間表格區域被壓縮
10. 關閉視窗後重開，確認大小與位置與關閉前相同
11. （選做）處理中關閉視窗，確認程式正常結束、無例外堆疊輸出

## 3. 監控項目

- **每日額度（RPD）**：`gemini-3.1-flash-lite` 每天 500 次請求，重置時間約為台灣時間早上 8 點。完整資訊見 `GEMINI_QUOTA.md`（額度數據已用化名描述，避免曝光真實 Google 帳號）
- **單張卡片處理時間**：約 1～1.5 分鐘／張，刻意設計成單一 worker thread 循序處理，避免撞到瞬時速率限制（RPM 15）
- **欄位寫回正確性**：每次完成後抽查「中文」/「文法結構」欄位內容、`AnkiExplainer` 是否已清空
- **記錄檔功能**：等使用者回覆三個澄清問題後才開始實作，不要自行假設欄位格式直接動手寫

## 相關檔案位置一覽（相對於專案根目錄）
- 主程式：`review_helper_gui.py`
- 多益專屬 prompt 範本：`examples/toeic_dataset.txt`
- Gemini 配額紀錄（已去識別化）：`GEMINI_QUOTA.md`
- 卡片模板備份（gitignored，僅本機）：`backups/`

> 備註：本檔案會被 push 到使用者的公開 fork，因此刻意不寫入真實姓名、作業系統使用者名稱、絕對檔案路徑等個人資訊。若要查詢這些細節，請改看本機 memory 或直接詢問使用者。
